import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ABABA GROUP LIMITED - Multi-Sector Business Solutions in South Sudan',
  description: 'Your trusted partner in Construction, ICT, Logistics, Agribusiness & More. Powering progress across South Sudan & East Africa.',
  keywords: 'South Sudan business, construction, ICT, logistics, agribusiness, ABABA GROUP',
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
